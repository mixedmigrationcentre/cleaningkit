% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_data.R
\name{read_loop_data}
\alias{read_loop_data}
\title{Read Loop/Roster Dataset}
\usage{
read_loop_data(
  filename,
  kobo_survey,
  sheet,
  loop_name,
  uuid_column = "_submission__uuid",
  submission_time_column = "_submission__submission_time"
)
}
\arguments{
\item{filename}{Path to the Excel file containing the loop data.}

\item{kobo_survey}{A dataframe containing the Kobo survey sheet (used for
identifying numeric columns).}

\item{sheet}{Sheet number or name to read from the Excel file.}

\item{loop_name}{A string label for this loop (e.g., "household_roster",
"children"). This is stored in a \code{df_name} column.}

\item{uuid_column}{Name of the parent UUID column in the loop data
(default is "_submission__uuid").}

\item{submission_time_column}{Name of the submission time column in the loop
data (default is "_submission__submission_time").}
}
\value{
A dataframe with a composite UUID, dates converted, and numeric
columns cast to numeric.
}
\description{
Reads a loop or roster dataset from an Excel file, converts date and datetime
columns, generates a composite UUID (row number + parent UUID), and converts
integer/decimal columns to numeric.
}
