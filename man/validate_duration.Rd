% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_duration.R
\name{validate_duration}
\alias{validate_duration}
\title{Validate Survey Duration}
\usage{
validate_duration(
  dataset,
  column_to_check = "_duration",
  uuid_column = "uuid",
  log_name = "duration_log",
  lower_bound = 15,
  upper_bound = 60
)
}
\arguments{
\item{dataset}{A dataframe or a list containing a dataframe named \code{checked_dataset}.}

\item{column_to_check}{The name of the column containing the duration in seconds. Default is \code{"_duration"}.}

\item{uuid_column}{The name of the column containing the unique identifier. Default is \code{"uuid"}.}

\item{log_name}{The name of the log to create in the list. Default is \code{"duration_log"}.}

\item{lower_bound}{The lower threshold for duration in minutes. Default is \code{15}.}

\item{upper_bound}{The upper threshold for duration in minutes. Default is \code{60}.}
}
\value{
A list containing the original dataset and the new log dataframe.
}
\description{
Checks whether the survey duration (typically stored in a column like \verb{_duration} in seconds)
falls within specified lower and upper bounds (in minutes). It generates a validation log
of surveys that are either too short or too long.
}
