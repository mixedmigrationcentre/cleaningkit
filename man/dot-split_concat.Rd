% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_other_response.R
\name{.split_concat}
\alias{.split_concat}
\title{Split a space-joined multi-word token string using a known vocabulary}
\usage{
.split_concat(concat, vocab)
}
\arguments{
\item{concat}{Space-joined string of selected values.}

\item{vocab}{Character vector of all valid tokens for this question.}
}
\value{
Character vector of matched tokens (in original order).
}
\description{
The concat column in ONA exports joins selected values with spaces. When
values are multi-word labels (e.g. "Travel in a group Plan my journey
carefully Other") a naive whitespace split breaks them. This function
greedily matches the longest known token from \code{vocab} (sorted by
descending word count), case-insensitively, so multi-word labels are
correctly identified as atomic tokens.
}
\keyword{internal}
