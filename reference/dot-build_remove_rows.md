# Build log rows for a REMOVE action

Build log rows for a REMOVE action

## Usage

``` r
.build_remove_rows(
  x,
  dataset,
  uuid_column,
  sm_separator,
  skip_label_row = TRUE,
  tool_choices = NULL
)
```
