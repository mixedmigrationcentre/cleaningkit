# Build log rows for a RECODE action

Build log rows for a RECODE action

## Usage

``` r
.build_recode_rows(
  x,
  dataset,
  tool_choices,
  uuid_column,
  sm_separator,
  skip_label_row = TRUE
)
```
