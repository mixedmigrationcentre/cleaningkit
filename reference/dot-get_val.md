# Look up current value from dataset by uuid and column

Look up current value from dataset by uuid and column

## Usage

``` r
.get_val(dataset, uuid_column, uuid, column, skip_label_row = TRUE)
```
