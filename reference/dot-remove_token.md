# Remove a token from a space-joined concat using vocabulary-aware splitting

Remove a token from a space-joined concat using vocabulary-aware
splitting

## Usage

``` r
.remove_token(concat, token, vocab)
```
